rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /vaccines/{businessId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == businessId;
    }
  }
}
